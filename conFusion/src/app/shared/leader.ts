import { CoreEnvironment } from '@angular/compiler/src/compiler_facade_interface';

export class Leader {
    id: string;
    name: string;
    image: string;
    designation: string;
    abbr: string;
    featured: boolean;
    description: string;
    
}